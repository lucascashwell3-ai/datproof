{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push *)",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && pytest tests/ -q || { echo 'datproof tests are failing — fix before pushing (see the verify-work agent for the full checklist, including the as_of/source rule).' >&2; exit 2; }"
          }
        ]
      }
    ]
  }
}
